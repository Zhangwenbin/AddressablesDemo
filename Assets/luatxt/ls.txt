

LUAFamilyProxy={};

-- function LUAFamilyProxy:startHotfix ()
--   xlua.private_accessible(CS.FamilyProxy)
--     xlua.hotfix(CS.FamilyProxy, 'GetFamilyGoods', 
--     function(self)
--     CS.UnityEngine.Debug.Log("lua FamilyProxy GetFamilyGoods");
sIndex;
--                 spegood.itemId = data.specialGoods.itemId;
--                 spegood.unlockLevel = 0;
--                 spegood.sellprice =CS.Tables.Tfamilyshop.GetValueByIntKey ( spegood.index ).Sell;
--                 spegood.buyprice = 0;
--                 spegood.type = 2;
--                 spegood.SonType = CS.Tables.Tfamilyshop.GetValueByIntKey ( spegood.index ).Itemtype;
--                 normals:Add ( spegood );
--             end

--             for k,v in pairs(CS.Tables.Tfamilyshop.DicsTable) do
                                     
--                 if  v.Type == 1 or v.Type == 3 then
                    
--                     local good = CS.FamilyProxy.FamilyShopItemData();
       
--                     good.index = v.Id;
--                     good.itemId = v.Item;
--                     good.unlockLevel = v.Unlock;
--                     good.sellprice = v.Sell;
--                     good.buyprice = v.Buy;
--                     good.type = v.Type;
--                     good.SonType =v.Itemtype;

--                     for   i = 0, data.normalGoods.Count-1  do
                    
--                         if ( data.normalGoods [ i ].goodsIndex == v.Id ) then
                            
--                             if  v.Type == 3  then                               
--                                 good.leftCount = -1;                              
--                             else                               
--                                 good.leftCount = data.normalGoods [ i ].inventory;
--                             end

--                             data.normalGoods:Remove ( data.normalGoods [ i ] );
--                             break;
                            
--                         end
--                     end
--                     if  v.Type == 3  then                               
--                       good.leftCount = -1;                              
                 
--                      end
                  
--                     normals:Add ( good );
                    
--                   end
--               end
--               CS.ProxyManager.Instance:GetProxy("FamilyProxy").normals=normals
--                 LUAUIBase:SendEvent ( "GetFamilyGoods" );
--           end,
--           CS.tutorial.FamilyShop()
        
--             );
           
--     end
--     )

-- end

-- return LUAFamilyProxy:startHotfix();

